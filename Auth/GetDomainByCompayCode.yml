info:
  name: GetDomainByCompayCode
  type: http
  seq: 1

http:
  method: GET
  url: https://uat-followonecrm.com/api/v1/verify-company-code/{company_code}
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
