info:
  name: edit-Order
  type: http
  seq: 2

http:
  method: GET
  url: "{{sale_uat}}/api/v1/order/edit-api/1"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
