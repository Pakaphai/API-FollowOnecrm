info:
  name: notifications/unread-count
  type: http
  seq: 3

http:
  method: GET
  url: "{{sale_uat}}/api/v1/notifications/unread-count"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
