info:
  name: list-debt-reduction
  type: http
  seq: 1

http:
  method: GET
  url: "{{sale_uat}}/api/v1/debt-reduction/list"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
