info:
  name: get-order-item
  type: http
  seq: 3

http:
  method: GET
  url: "{{sale_uat}}/api/v1/debt-reduction/order-items/12"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
