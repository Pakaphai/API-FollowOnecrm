info:
  name: delete-relationship-detail
  type: http
  seq: 3

http:
  method: DELETE
  url: "{{sale_uat}}/api/v1/relationship-detail/delete-contact/4"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  ## 14. `DELETE /api/v1/relationship-detail/delete-contact/{id}`

  ลบนัดหมาย (soft delete) — **`{id}` = `relationship_detail_id`**

  ```bash
  curl -s -X DELETE "$BASE/relationship-detail/delete-contact/88" -H "$AUTH" -H "Accept: application/json" | jq
  ```

  | HTTP | ก้อนที่ได้ |
  |:---:|---|
  | `200` | `{"success": true, "message": "ลบข้อมูลเรียบร้อยแล้ว"}` |
  | `404` | `{"success": false, "message": "ไม่พบข้อมูลที่ต้องการลบ"}` |
  | `500` | `{"success": false, "message": "...", "error": "..."}` |

  ### อธิบายตัวแปรใน response ทุกตัว

  | ฟิลด์ | ชนิด | อธิบาย |
  |---|---|---|
  | `success` | boolean | 🔴 ตัวชี้ขาดว่าลบสำเร็จหรือไม่ — มีครบทุกกรณี (200 / 404 / 500) |
  | `message` | string | ข้อความภาษาไทย · ตอน 500 เป็นข้อความที่ระบบแปลงให้อ่านง่ายแล้ว (เช่น "ข้อมูลนี้ถูกใช้งานอยู่...") โชว์ผู้ใช้ได้ |
  | `error` | string | **มีเฉพาะตอน 500** — ข้อความ exception ดิบ ใช้ debug เท่านั้น |

  > เป็น **soft delete** (`is_delete = true`) ข้อมูลยังอยู่ในฐานข้อมูล แค่ไม่ถูกดึงมาแสดงอีก

  > 🐞 **มีผลข้างเคียงที่ไม่ตั้งใจ** — นอกจากลบใบที่ระบุ โค้ดยังไล่ลบ detail ทุกใบที่ `relationship_id == {id}` ด้วย
  > คือเอา **id ของ detail ไปเทียบกับคอลัมน์ relationship_id** ถ้าบังเอิญมี relationship ที่ id ตรงกับเลขนี้
  > นัดหมายของแผนนั้นจะโดนลบไปด้วยทั้งชุด — ควรใช้ด้วยความระวังจนกว่าจะแก้
  > (การลบไฟล์รูปใช้ `disk('public')` เหมือนเส้น 3 จึงไม่โดนไฟล์บน S3)

  ---


  ---
