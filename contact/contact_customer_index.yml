info:
  name: contact_customer_index
  type: http
  seq: 2

http:
  method: GET
  url: "{{sale_uat}}/api/v1/customer/contact/customer/3"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
