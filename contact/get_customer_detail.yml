info:
  name: get_customer_detail
  type: http
  seq: 3

http:
  method: GET
  url: "{{sale_uat}}/api/v1/getcustomer/detail/byid/4"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
