info:
  name: edit_quotation
  type: http
  seq: 3

http:
  method: GET
  url: "{{sale_uat}}/api/v1/quotation/edit/8"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
