info:
  name: edit_quotation
  type: http
  seq: 3

http:
  method: GET
  url: "{{sale_uat}}/api/v1/quotation/edit/8"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |
  # API — ข้อมูลสำหรับฟอร์มแก้ไขใบเสนอราคา

  ```
  GET /api/v1/quotation/edit/{id}
  Authorization: Bearer <token>
  ```

  | | |
  |---|---|
  | Route | [`routes/api/api_v1.php`](../routes/api/api_v1.php) — อยู่ในกลุ่ม `auth_api` |
  | Controller | [`Api\ApiQuotationController::qutation_edit_api()`](../app/Http/Controllers/Api/ApiQuotationController.php) |
  | ส่งกลับไปบันทึกที่ | [`POST quotation/update/{id}`](api-quotation-update.md) |
  | ข้อมูลตั้งต้นของฟอร์ม | [api-quotation-preload.md](api-quotation-preload.md) |

  ---

  ## โครง response

  ```json
  {
    "status": "success",
    "quotation_headers": {
      "id": 12,
      "quotation_number": "QT-2026-0012",
      "credit_note_numbers": ["SR-202608-0004"],
      "quotation_date": "2026-08-22",
      "status": "open",
      "price_mode": "price",
      "total_before_discount": 47838.45,
      "total_after_discount": 47838.45,
      "usd_rate": { "rate": 32.9781, "rate_used": 33, "as_of": "2026-08-21", ... },
      "usd_rate_from_document": true,
      "total_after_discount_usd": 144965,
      "quotation_lines": [ ... ]
    },
    "customers": [ ... ],
    "products": [ ... ],
    "reserved_by_this_order": { "12": 5 },
    "conditions": [ ... ]
  }
  ```

  `credit_note_numbers` — เลขที่ใบลดหนี้ (`SR-xxxxxx-xxxx`) ของใบสั่งขายที่ผูกกับใบนี้
  ตัดใบที่ยกเลิก/ไม่อนุมัติออก · `[]` = ไม่มี หรือยังไม่ยืนยันเป็นใบสั่งขาย (รายละเอียดดู `list_quotation`)

  ---

  ## `products[]` + `reserved_by_this_order` — เพดานจำนวนของบรรทัด

  สินค้าแต่ละตัวใน `products[]` มีตัวเลขสต๊อกสามช่อง

  | ฟิลด์ | ความหมาย |
  |---|---|
  | `min_stock_level` | **ของจริงบนชั้น** (ชื่อคอลัมน์จริงใน DB) |
  | `reserved_qty` | **ของที่ใบอื่นจองไว้** — ใบที่เปิดบิลแล้วแต่ยังไม่ส่งของ |
  | `stock_qty` · `available_qty` | **คงเหลือ (พร้อมขาย)** = ของจริง − จอง ← ตัวที่ต้องโชว์เป็นคงเหลือ (สองชื่อ ค่าเดียวกัน) |
  | `low_stock_threshold` · `is_low_stock` | จุดแจ้งเตือนเหลือน้อยของสินค้าตัวนั้น และธงว่าต่ำกว่าเกณฑ์แล้วหรือยัง |

  `reserved_by_this_order` คือของที่ **ใบนี้เอง** จองไว้ แยกรายสินค้า `{ "<product_id>": จำนวน }`
  ก้อนว่าง `{}` เมื่อใบยังไม่ถึงขั้นเปิดบิล ซึ่งเป็นกรณีปกติของใบที่ยังแก้ได้

  > 📦 **เพดานจำนวนของบรรทัด** = `available_qty` **+** `reserved_by_this_order[product_id]`
  > ของที่ใบนี้จองไว้เองยังเอากลับมาใช้ในใบนี้ได้ ไม่ได้ถูกใบอื่นจอง ถ้าไม่บวกคืน ผู้ใช้จะเห็นเพดานต่ำกว่าจริง
  > (ของจริง 100 · ใบนี้จอง 10 · ใบอื่นจอง 20 → ขายได้ 70 แต่ใบนี้แก้ได้ถึง 80) — สูตรเดียวกับหน้าแก้ไขฝั่งเว็บ

  > `quotation_headers.status` เป็นคีย์อังกฤษ และมี `status_label` เป็นป้ายไทยชุดเดียวกับฝั่งเว็บมาให้ด้วย

  ---

  ## `quotation_lines[]` — รายการสินค้า

  | ฟิลด์ | หน่วย | หมายเหตุ |
  |---|---|---|
  | `id` | | รหัสบรรทัด (`quotation_lines.id`) |
  | `product_id` · `uom_code` | | `uom_code` ตัดช่องว่างท้ายให้แล้ว |
  | `qty` · `unit_price` · `unit_price_total` · `discount` · `manual_discount` · `total_line` | **บาท** | หารร้อยมาให้แล้ว |
  | `usd_price` | **ดอลลาร์** | ราคาต่อหน่วยถอดกลับจากราคาบาท ÷ อัตราของใบนี้ |
  | `total_line_usd` | **ดอลลาร์** | ยอดบรรทัดเทียบ USD |
  | `options[]` | | ตัวเลือกสินค้าที่ลูกค้าเลือกไว้ — ดูหัวข้อถัดไป |

  > `usd_price` ถอดกลับมาให้**เฉพาะใบที่บันทึกอัตราไว้กับตัวเอง** (`usd_rate_from_document: true`)
  > ใบเก่าที่ยืมอัตราปัจจุบันมาแสดงจะได้ `null` เพราะถ้าถอดเป็น USD แล้วส่งกลับมาบันทึก
  > ราคาจะถูกปัดขึ้นจนเพี้ยนไปจากราคาบาทเดิม (ฐานข้อมูลเก็บราคาเป็นบาท ไม่ได้เก็บ USD รายบรรทัด)

  ---

  ## `quotation_lines[].options[]` — ตัวเลือกสินค้า

  ```json
  "options": [
    { "id": 3, "group_name": "Through-Spindle Coolant", "name": "TSC READY-3RD PARTY",
      "price": 168135, "usd": "5095.0000" },
    { "id": 8, "group_name": "Spindle", "name": "10K-40T-IN",
      "price": 250635, "usd": "7595.0000" }
  ]
  ```

  | ฟิลด์ | ความหมาย |
  |---|---|
  | `id` | id ใน `product_options` — **ส่งกลับไปที่ `option_ids` ตอนบันทึกได้ตรง ๆ** |
  | `group_name` · `name` | ชื่อหมวดหมู่/ตัวเลือก **ณ วันที่ออกเอกสาร** |
  | `price` | ราคาที่บวกเพิ่ม หน่วยเป็น **บาท** (หารร้อยมาให้แล้ว เหมือนช่องเงินอื่นในเส้นนี้) |
  | `usd` | ราคาที่บวกเพิ่มสกุล USD (`null` = ไม่ได้ตั้งราคา USD ไว้) |

  เรียงตามลำดับที่ผู้ใช้เลือกไว้ตอนบันทึก · บรรทัดที่ไม่มีตัวเลือกได้ `[]`

  > ค่าทั้งหมดเป็น **สำเนา** ที่บันทึกไว้ตอนออกเอกสาร (ตาราง `quotation_line_options`)
  > ไม่ได้อ่านสดจากตัวสินค้า — แก้ชื่อหรือราคาที่หน้าสินค้าภายหลัง ใบเก่ายังแสดงค่าเดิมเสมอ

  ---

  ## ราคาของบรรทัดมาเป็นสองค่า

  | ฟิลด์ | ความหมาย |
  |---|---|
  | `unit_price` · `usd_price` | **ราคาสินค้าล้วน** ไม่รวมตัวเลือก |
  | `unit_price_total` · `usd_price_total` | **ราคาต่อหน่วยที่ลูกค้าจ่ายจริง** (สินค้า + ตัวเลือก) |

  - เอาไป**แสดงผล** ใช้ `unit_price_total`
  - เอาไป**ยิงบันทึกต่อ** ที่ `quotation/update/{id}` ใช้ `unit_price` / `usd_price`

  ส่งกลับไปดิบ ๆ ได้เลย เซิร์ฟเวอร์บวกราคาตัวเลือกให้ใหม่จาก `option_ids` ที่ส่งไปด้วยกัน
  ราคาจึงไม่บานขึ้นทุกครั้งที่กดบันทึก

  `total_line` / `total_line_usd` เป็นยอดเต็มของบรรทัด (รวมตัวเลือกแล้ว) เหมือนเดิม
  และ `options[].price` ก็รวมอยู่ใน `unit_price_total` แล้ว ไม่ต้องบวกซ้ำ

  > ⚠️ ตอนส่ง `option_ids` กลับไปบันทึก **ตัวเลือกต้องเป็นของสินค้าในบรรทัดนั้น**
  > ส่ง id ของตัวเลือกที่สังกัดสินค้าตัวอื่นจะได้ `422` ที่คีย์ `<lines>.N.option_ids.M`
