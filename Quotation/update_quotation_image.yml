info:
  name: update_quotation_image
  type: http
  seq: 9

http:
  method: POST
  url: "{{sale_uat}}/api/v1/quotation/with-images/update/5"
  body:
    type: multipart-form
    data:
      - name: customer_id
        type: text
        value: "2"
      - name: quotation_date
        type: text
        value: 2026-07-30
      - name: additional_amount
        type: text
        value: "100"
      - name: quotation_image
        type: file
        value:
          - C:\Users\Softa-P\Pictures\Screenshots\image_commission_landingpage.png
        contentType: image/png
      - name: note
        type: text
        value: สามารถจัดส่งสินค้าได้ภายในเดือนหน้า
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
