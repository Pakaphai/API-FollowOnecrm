info:
  name: cancelled_quotation
  type: http
  seq: 7

http:
  method: POST
  url: "{{sale_uat}}/api/v1/quotation/4/status/cancelled"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
