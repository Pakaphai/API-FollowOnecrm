info:
  name: preload_quotation
  type: http
  seq: 5

http:
  method: GET
  url: "{{sale_uat}}/api/v1/quotation/preload"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
