info:
  name: list_quotation
  type: http
  seq: 4

http:
  method: GET
  url: "{{sale_uat}}/api/v1/quotation/list"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
