info:
  name: list_quotation
  type: http
  seq: 4

http:
  method: GET
  url: "{{sale_uat}}/api/v1/quotation/list?page=1&per_page=20&q="
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |
  ## การแบ่งหน้า (pagination)

  เส้นนี้แบ่งหน้าเสมอ ไม่ส่งอะไรมาก็ได้ 20 รายการแรก

  | พารามิเตอร์ | ชนิด | ค่าเริ่มต้น |
  |---|---|---|
  | `page` | integer >= 1 | 1 |
  | `per_page` | integer 1-200 | 20 |
  | `q` | string <= 100 | - |

  ส่ง `per_page` เกิน 200 หรือ `q` ยาวเกิน 100 ตัว จะได้ 422

  ## การค้นหา (q)

  `q` ถูกส่งไปกรองที่ฐานข้อมูล **ก่อน** แบ่งหน้า จึงค้นเจอทุกรายการที่ตรงคีย์เวิร์ด
  ไม่ใช่เฉพาะหน้าที่โหลดมาแล้ว ค้นแบบไม่แยกตัวพิมพ์เล็กใหญ่ (ILIKE) และมีคำนี้อยู่ตรงไหนก็ได้

  ค้นจาก: เลขที่ใบเสนอราคา - ชื่อลูกค้า - รหัสลูกค้า - ชื่อผู้ติดต่อ

  meta ที่แนบกลับมาทุกครั้ง (ระดับเดียวกับ key ของรายการ):

  ```json
  { "page": 1, "size": 20, "total_elements": 137, "total_pages": 7,
    "has_next": true, "has_prev": false }
  ```

  ต้องวนดึงต่อตาม `has_next` / `total_pages` ถ้าอยากได้ครบทุกรายการ
  รายละเอียดทั้งหมดอยู่ที่ docs/api-reference.md หัวข้อ "การแบ่งหน้า (pagination)"

  ## ป้ายสถานะ (`status` / `status_en`)

  `status_en` คือคีย์อังกฤษ ส่วน `status` คือป้ายภาษาไทยชุดเดียวกับฝั่งเว็บ (`OrderModel::STATUS_LABELS`)

  **สถานะของใบเสนอราคาเดินได้ทั้งสายแล้ว ไม่ได้หยุดที่ `confirmed`** — ระบบซิงก์สถานะใบเสนอราคา
  ให้เดินตามใบสั่งขายที่ผูกกันทุกครั้งที่ใบสั่งขายเปลี่ยนขั้น จึงเจอค่าอย่าง `pending_billing`
  `dispatch` `shipped` `completed` ในเส้นนี้ได้

  | `status_en` | `status` (ป้ายไทย) |
  |---|---|
  | `open` | เปิดใบเสนอราคา |
  | `confirmed` | ยืนยันใบเสนอราคา |
  | `pending_billing` | เปิดบิล |
  | `dispatch` | รอส่งของ |
  | `shipped` | รอบัญชีแจ้งยอด |
  | `pending_slip` | รอแนบสลิป |
  | `pending_receipt` | รอออกใบเสร็จ |
  | `completed` | การขายเสร็จสิ้น |
  | `rejected` | สินค้าไม่เพียงพอ |
  | `cancelled` | ยกเลิก |

  > ⚠️ ของเดิมแปลเป็นไทยแค่ `open` กับ `confirmed` ค่าอื่นหลุดออกไปเป็นคีย์อังกฤษดิบ
  > ตอนนี้ได้ป้ายไทยครบทุกค่า · ขั้น `approved` (ตรวจสอบสินค้า) ถูกตัดออกจากกระบวนการขายแล้ว

  ## โครงสร้าง Response (ตัวอย่าง quotation_lines ที่มี options)

  ```json
  {
    "status": "success",
    "quotation_headers": [
      {
        "id": 10,
        "customer_id": 4,
        "quotation_number": "QT-2026-0010",
        "quotation_date": "2026-08-22",
        "status": "เปิดใบเสนอราคา",
        "status_en": "open",
        "total_before_discount": 70118.40,
        "total_discount": 0.00,
        "total_after_discount": 70118.40,
        "quotation_lines": [
          {
            "id": 13,
            "product_id": 6,
            "qty": 1,
            "discount_type": "amount",
            "manual_discount": 0,
            "uom_code": "EA",
            "unit_price": 31181.70,
            "discount": 0,
            "total_line": 31181.70,
            "options": [
              {
                "id": 1,
                "group_name": "สี",
                "name": "สีแดง",
                "price": 50.00,
                "usd": 1.50
              }
            ]
          }
        ],
        "quotation_conditions": []
      }
    ],
    "page": 1,
    "size": 20,
    "total_elements": 1,
    "total_pages": 1,
    "has_next": false,
    "has_prev": false
  }
  ```

